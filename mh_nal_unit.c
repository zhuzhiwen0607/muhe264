#include "mh_nal_unit.h"
#include "mh_error.h"

mh_result_t mh_nal_unit_main(mh_cycle_queue_p queue, mh_uint8_t *start, mh_int32_t size)
{
    if (!queue || !start || size <= 0)
        return MH_ERROR;


}
